// Single source of truth for the user-visible app name.
// NOTE: AsyncStorage keys, scheme, slug, bundle/package identifiers are intentionally
// NOT changed here (see `app.json` and session storage keys) to avoid logouts.
export const APP_NAME = 'PhysTech';

